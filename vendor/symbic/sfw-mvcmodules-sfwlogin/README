Howto add the module to a project:

1. install by adding the module to the projects composer file

...
	"require": {
	...
		"symbic/sfw-mvcmodules-sfwlogin": "*",
	...
	}
...

2. register the module with the frontcontroller within the application config

	<controllerdirectory>
		...
		<sfwlogin><zf:const zf:name="VENDOR_PATH"/>/symbic/sfw-mvcmodules-sfwlogin/modules/sfwlogin/controllers</sfwlogin>
		...
	</controllerdirectory>

3. Add some extra routes if you like to use clean urls

	<router>
		<routes>
			<login>
				<route>/login/*</route>
				<defaults>
					<module>sfwlogin</module>
					<controller>login</controller>
					<action>index</action>
				</defaults>
			</login>
			<logout>
				<route>/logout</route>
				<defaults>
					<module>sfwlogin</module>
					<controller>logout</controller>
					<action>index</action>
				</defaults>
			</logout>
			<home>
				<route>/home</route>
				<defaults>
					<module>sfwdashboard</module>
					<controller>index</controller>
					<action>index</action>
				</defaults>
			</home>
		</routes>
	</router>