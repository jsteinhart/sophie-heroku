To be implemented
<?php
return;

$module = 'sfwsysadmin';
$this->headTitle('Sessions', 'PREPEND');
$this->headLink()->appendStylesheet($this->sfwModuleAssetUrl('module.css', $module));
$this->inlineScript()->appendFile($this->sfwModuleAssetUrl('module.js', $module));

$this->headTitle('System Administration', 'PREPEND');
$this->headTitle('Logs', 'PREPEND');
$this->headTitle('Database', 'PREPEND');
?>
<h1>Database Logs</h1>
<div class="table-responsive">
<table id="logDatabaseList" class="table table-striped table-hover">
<thead>
	<tr><th>Date</th><th>Content</th></tr>
</thead>
<tbody>
<?php
foreach ($this->entries as $entry)
{
	echo '<tr>';
		echo '<td>';
		echo '</td>';
		echo '<td>';
		print_r($entry);
		echo '</td>';
	echo '</tr>';
}
?>
</tbody>
</table>
</div>