<?php
$module = 'sfwsysadmin';
$this->headLink()->appendStylesheet($this->sfwModuleAssetUrl('module.css', $module));
$this->inlineScript()->appendFile($this->sfwModuleAssetUrl('module.js', $module));

$this->headTitle('System Administration', 'PREPEND');
$this->headTitle('Mail', 'PREPEND');
$this->headTitle('Queue', 'PREPEND');
?>
<h1>Mail Queue</h1>

<div class="table-responsive">
<table id="mailQueueList" class="table table-striped table-hover">
<thead>
	<tr><th>Name</th><th>Path</th><th>Details</th></tr>
</thead>
<tbody>
<?php
foreach ($this->result as $mail):
	echo '<tr>';
		echo '<td>';
		print_r($mail);
		echo '</td>';
	echo '</tr>';
endforeach;
?>
</tbody>
</table>
</div>